<div class="placeholder-container {{if sorted 'sort-caret'}}">
  <div class="sort-header-name" title={{content.key}}>
    {{content.key}}
  </div>
  {{#if sorted}}
    <span class="sort">
      <b class="caret up"></b>
      <b class="caret down"></b>
    </span>
  {{/if}}
</div>
